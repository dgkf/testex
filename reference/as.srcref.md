# Convert to [srcref](https://rdrr.io/r/base/srcfile.html)

Convert to [srcref](https://rdrr.io/r/base/srcfile.html)

## Usage

``` r
as.srcref(x)

# S3 method for class 'character'
as.srcref(x)
```

## Arguments

- x:

  an object to coerce

## Value

A [srcref](https://rdrr.io/r/base/srcfile.html)

## Methods (by class)

- `as.srcref(character)`: Convert from a `srcref_key` to a
  [srcref](https://rdrr.io/r/base/srcfile.html) object
