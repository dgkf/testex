# Determine which symbol to use by default when testing examples

Determine which symbol to use by default when testing examples

## Usage

``` r
get_example_value()
```

## Value

The value of the last test expression
